# Context 0, mask 3. MOSI=uio0, SCK=uio1, mode 0, MSB first.
# Chip select is managed by the host/application separately.
    LDI 3
    DIR
byte:
    PULL
    LDX 8
bit:
    OUTBIT 0
    SET 1, 1
    SET 1, 0
    DJNZ bit
    JMP byte
